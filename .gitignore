node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example
.DS_Store
playwright-report
test-results
coverage

# Large source cookbook PDFs (extracted into src/lib/data/cookbook.ts)
assets/*.pdf
assets/*.xlsx

# Adobe Illustrator brand-source art (large binaries — never commit)
*.ai
assets/*.ai

# Supabase CLI local link state
supabase/.temp/

# Android TWA signing key + PWABuilder secrets — NEVER commit
signing.keystore
*.keystore
*.jks
signing-key-info.txt
